Implements discrete least squares polynomial approximation, together with two benchmarks.

The function leastSquares takes as input the array x of points on which to plot the polynomial, the arrays x_nodes and y_nodes containig the nodes coordinates, and the degree n of the interpolating polynomial. It returns the array p of the values that the approximating polynomial takes on x.
